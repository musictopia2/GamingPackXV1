namespace BasicGameFrameworkLibrary.BasicGameDataClasses
{
    public enum EnumSuggestedOrientation //not sure if this is needed.   will leave for now but could take out for next version.
    {
        Landscape, Portrait
    }
}