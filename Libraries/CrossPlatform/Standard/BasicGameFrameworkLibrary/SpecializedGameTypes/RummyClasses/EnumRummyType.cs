namespace BasicGameFrameworkLibrary.SpecializedGameTypes.RummyClasses
{
    public enum EnumRummyType //started because of optong.  if something else is needed, think about renaming (?)
    {
        Colors = 1,
        Runs = 2,
        Sets = 3
    }
}